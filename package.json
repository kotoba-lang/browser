{
  "name": "kotoba-browser",
  "private": true,
  "version": "0.1.0",
  "description": "kotoba-lang/browser QuickJS/QuickJS-NG WASM guest execution build pipeline.",
  "scripts": {
    "compile:node-test": "shadow-cljs compile node-test",
    "compile:browser-demo": "shadow-cljs compile browser-demo",
    "watch:browser-demo": "shadow-cljs watch browser-demo",
    "serve:browser-demo": "node scripts/demo-server.js",
    "demo:browser-demo": "npm run compile:browser-demo && npm run serve:browser-demo",
    "test:cljs": "npm run compile:node-test && node target/node-tests.js",
    "test:clj": "clojure -M:test",
    "compile:smoke": "shadow-cljs compile visual-smoke",
    "compile:webgpu-smoke": "shadow-cljs compile webgpu-smoke",
    "smoke:visual": "npm run compile:smoke && clojure -M:smoke visual",
    "smoke:webgpu": "npm run compile:webgpu-smoke && clojure -M:smoke webgpu",
    "check": "npm run test:clj && npm run test:cljs && npm run smoke:visual && npm run smoke:webgpu"
  },
  "dependencies": {
    "quickjs-emscripten-core": "^0.32.0",
    "@jitl/quickjs-singlefile-cjs-release-sync": "^0.32.0"
  },
  "devDependencies": {
    "shadow-cljs": "^2.28.20"
  }
}
